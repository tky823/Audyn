seed: 0

distributed:
  enable: true
  nodes:  # number of nodes
  nproc_per_node:  # number of processes per node
  backend: gloo
  init_method:

  # multi-node training parameters
  rdzv_id:
  rdzv_backend:
  rdzv_endpoint:
  max_restarts:

cudnn:
  benchmark:
  deterministic:

amp:
  enable: false
  dtype:

accelerator: cpu
