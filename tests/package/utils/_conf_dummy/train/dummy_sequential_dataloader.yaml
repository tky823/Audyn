dataset:
  train:
    _target_: dummy.utils.data.DummySequentialDataset
    num_features:
    min_length:
    size:
  validation:
    _target_: dummy.utils.data.DummySequentialDataset
    num_features: ${..train.num_features}
    min_length: ${..train.min_length}
    size:

dataloader:
  train:
    _target_:
    batch_size:
    shuffle: false
  validation:
    _target_:
    batch_size:
    shuffle: false

key_mapping:
  train:
    input:
      input: input
    output: criterion_input
  validation: ${.train}

resume:
  continue_from:

output:
  exp_dir: "./exp"
  tensorboard_dir: "./tensorboard"
  save_checkpoint:
    iteration:
      every: 1
      path: ${...exp_dir}/model/iteration{iteration}.pth
    epoch:
      every: 1
      path: ${...exp_dir}/model/epoch{epoch}.pth
    last:
      path: ${...exp_dir}/model/last.pth
    best_epoch:
      path: ${...exp_dir}/model/best_epoch.pth

record:

steps:
  epochs:
  iterations:
  lr_scheduler: iteration
