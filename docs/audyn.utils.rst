audyn.utils
===========

``audyn.utils`` provides utilities for ``Audyn``.

Submodules
----------

.. toctree::
   :maxdepth: 1

   audyn.utils.data
   audyn.utils.music

Cache directory
---------------

By default, `$HOME/.cache/audyn` is used as a cache directory of `Audyn`.
To change the cache directory, set `AUDYN_CACHE_DIR` as an environmental variable.
