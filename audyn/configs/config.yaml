defaults:
  - system: defaults
  - preprocess: defaults
  - data: defaults
  - train: defaults
  - test: defaults
  - model: defaults
  - optimizer:
  - lr_scheduler: defaults
  - criterion:
  - _self_

hydra:
  job:
    chdir: false
