defaults:
  - defaults
  - _self_

distributed:
  enable: true
  nodes:  # number of nodes
  backend: nccl
  init_method: "env://"
cudnn:
  benchmark: true
  deterministic: false
amp:
  enable: false
accelerator: cuda
