audio:
  iteration:
    every: 1000
    sample_size: 1
    sample_rate: ${data.audio.sample_rate}
    key_mapping:
      output:
        separated: "Separated source (iteration)"
      reference:
        source: "Target source (iteration)"
    transforms:
  epoch:
    every: 1
    sample_size: 1
    sample_rate: ${data.audio.sample_rate}
    key_mapping:
      validation:
        output:
          separated: "Separated source (epoch)"
        reference:
          source: "Target source (epoch)"
    transforms:
